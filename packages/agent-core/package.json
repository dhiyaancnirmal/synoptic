{
  "name": "@synoptic/agent-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@synoptic/types": "workspace:*",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.21.0"
  }
}
